TABLAS:  

	CREATE TABLE PERSONAS (
	ID_PERSONA INT IDENTITY(1,1) PRIMARY KEY NOT NULL,  
	IDENTIFICACION INT NOT NULL,
	NOMBRE_1 VARCHAR(50)NOT NULL, 
	NOMBRE_2 VARCHAR(50)NULL, 
	APELLIDO_1 VARCHAR(50)NOT NULL, 
	APELLIDO_2 VARCHAR(50)NULL,
	EDAD INT NOT NULL,
	ROL INT NOT NULL
	);

	CREATE TABLE USUARIOS (
	ID_USUARIO INT IDENTITY(1,1) PRIMARY KEY NOT NULL,  
	ID_PERSONA INT NOT NULL, 
	USUARIO VARCHAR(50)NOT NULL,
	CONTRASEÑA VARCHAR(51) NOT NULL
	);

	CREATE TABLE ROLES_PERMISOS(
	ID_ROL_PER INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	ID_ROL INT NOT NULL,
	ID_MODULO INT NOT NULL,
	POST BOOLEAN NULL, 
	ROLES INT NULL
	);

	CREATE TABLE ROLES(
	ID_ROL INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	ROL VARCHAR(50)NOT NULL
	);


	CREATE TABLE MODULOS(
	ID_MODULOS INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	MODULOS VARCHAR(50)NOT NULL
	);




	CREATE TABLE CLIENTE (
	ID_CLIENTE INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	ROL INT NULL
	);
	CREATE TABLE EMPLEADO (
	ID_EMPLEADO INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	ROL INT NULL
	);

	CREATE TABLE ADMINISTRADOR (
	ID_ADMINISTRADOR INT IDENTITY(1,1) PRIMARY KEY NOT NULL,
	ROL INT NULL,
	);





	CREATE TABLE PEDIDO (
	ID_PEDIDO INT IDENTITY(1,1) PRIMARY KEY NOT NULL, 
	PEDIDO VARCHAR(50)NOT NULL, 
	ID_CLIENTE INT NULL,
	ID_VEHICULOS INT NULL
	)
	 
	CREATE TABLE FACTURA (
	ID_FACTURA INT IDENTITY(1,1) PRIMARY KEY NOT NULL,  
	ID_PEDIDO INT NULL,
	FECHA_FACTURA DATE
	);


	CREATE TABLE VEHICULOS (
	ID_VEHICULOS INT IDENTITY(1,1) PRIMARY KEY NOT NULL,  
	NOMBRE VARCHAR(50)NOT NULL,
	PRECIO VARCHAR(50)NOT NULL, 
	ID_CATEGORIA INT NULL, 
	ID_CONCESIONARIO INT NULL
	);


	CREATE TABLE CATEGORIA (
	ID_CATEGORIA INT IDENTITY(1,1) PRIMARY KEY NOT NULL, 
	NOMBRE VARCHAR(50)NOT NULL
	);


	CREATE TABLE CONCESCIONARIO (
	ID_CONCESIONARIO INT IDENTITY(1,1) PRIMARY KEY NOT NULL, 
	NOMBRE VARCHAR(50)NOT NULL,
	DIRECCION VARCHAR(50)NOT NULL, 
	TELEFONO INT NULL,
	EMAIL VARCHAR(60) NULL
	);


	ALTER TABLE PEDIDO ADD CONSTRAINT PK_ID_VEHICULOS FOREIGN KEY (ID_VEHICULOS) REFERENCES VEHICULOS (ID_VEHICULOS);
	ALTER TABLE FACTURA ADD CONSTRAINT PK_ID_PEDIDO FOREIGN KEY (ID_PEDIDO) REFERENCES PEDIDO (ID_PEDIDO);
	ALTER TABLE VEHICULOS ADD CONSTRAINT PK_ID_CATEGORIA FOREIGN KEY (ID_CATEGORIA) REFERENCES CATEGORIA (ID_CATEGORIA);
	ALTER TABLE VEHICULOS ADD CONSTRAINT PK_ID_CONCESIONARIO FOREIGN KEY (ID_CONCESIONARIO) REFERENCES CONCESCIONARIO (ID_CONCESIONARIO);
	ALTER TABLE PEDIDO ADD CONSTRAINT PK_ID_CLIENTE FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTE (ID_CLIENTE);


